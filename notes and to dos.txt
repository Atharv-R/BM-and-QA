

Keeping a text file here for notes and to-do items: 
-------------------------------------------------------------------------------------

- Make sure that the CD / PCD is working properly
    -Approach: Use an RBM architecture in our custom BM framework, train and test
      (if this does not work, something is off with our CD / PCD implementation)    
    - This also allows for testing the sampling stuff 

- Make sure the optimization of the node assignments is happening reasonably: 
    are we doing the right thing to get "close" to an RBM? 

- Pixel assignment to nodes in the graph: We need to map which pixel goes to 
    which node in the graph. This should be by "importance" in some sense..., 
    central nodes in the image should have high connectivity 


- Gradient tracking: What is it for and what does it do??

- I think we should delete the "heuristic vectorized" sampling function, 
    or at least not use it. 
    --> use the "coloring" version instead! 

- mean field update in bolmaqua? 
- modularize code more: 
    only keep the BM related stuff in bolmaqua, 
    or separate it out into custom_bms? 
    --> just code for "training a custom architecture BM" 
